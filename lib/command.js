var validate = require('./validate')

module.exports = Command

/**
 * A Command represents a memcache query, which can be a getter, setter, or
 * anything else. The Command handles validating parameters.
 */
function Command(params) {
}

